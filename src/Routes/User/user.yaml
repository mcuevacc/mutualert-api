## --- User Auth
UserAuth:
    resource: "route/auth.yaml"
    prefix: /auth/

## --- User Account
UserAccount:
    resource: "route/account.yaml"
    prefix: /account/

## --- User Profile
UserProfile:
    resource: "route/profile.yaml"
    prefix: /profile/

## --- User State
UserState:
    resource: "route/state.yaml"
    prefix: /state/

## --- User Session
UserSession:
    resource: "route/session.yaml"
    prefix: /session/